# 將成績寫入 Google 試算表

這個版本不需要把成績只存在 Render 或本機檔案。每次學生完成遊戲後，後端會把成績同步送到 Google 試算表。

## 1. 建立試算表

1. 到 Google Drive 建立一個新的 Google 試算表。
2. 建議命名為：`SDGs 綠色守護者成績紀錄`。

## 2. 貼上 Apps Script

1. 在試算表上方選單點選：`擴充功能` -> `Apps Script`。
2. 刪除原本內容。
3. 貼上本專案的 `google-apps-script-score-webhook.js` 內容。
4. 按儲存。

## 3. 部署成 Web App

1. 在 Apps Script 右上角點 `部署` -> `新增部署作業`。
2. 類型選 `網頁應用程式`。
3. `執行身分` 選 `我`。
4. `誰可以存取` 選 `任何知道連結的使用者`。
5. 按部署並授權。
6. 複製產生的 `網頁應用程式網址`。

## 4. 將網址設定到遊戲後端

把剛剛複製的網址設定成環境變數：

```text
GOOGLE_SHEET_WEBHOOK_URL=你的 Apps Script 網頁應用程式網址
```

如果使用其他名稱，也可以用：

```text
GOOGLE_APPS_SCRIPT_URL=你的 Apps Script 網頁應用程式網址
```

## 寫入欄位

試算表會自動建立 `成績紀錄` 工作表，欄位包含：

- 完成時間
- 班級
- 座號
- 學生
- 模式
- 分數
- 答對題數
- 總題數
- 秒數
- 對戰編號
- 對手
- 不熟悉內容
- 答題明細

## 注意

遊戲仍需要一個可執行後端的地方。若完全不使用 Render，就需要改用其他主機、學校伺服器，或下一步把整個遊戲改寫成 Google Apps Script 網頁應用程式。這次更新先完成「成績同步到 Google 試算表」的能力。
